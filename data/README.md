# data/

Local inputs only ,  nothing here is committed by default (`.gitignore`
excludes `data/*` except for this README and `.gitkeep`).

Drop your local copies of:
- AOI shapefile (if not using the GEE asset)
- Training-sample GeoJSON / shapefile (if you've exported the GEE
  Geometry Imports to disk)

See `../docs/setup_instructions.md` step 4 for the recommended workflow.

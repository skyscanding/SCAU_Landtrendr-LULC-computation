# Python dependencies for the GEE_LULC_SVM module.
# Tested against Python 3.10-3.12.

# Core GEE client ,  drives the same computation as the JS scripts
earthengine-api>=0.1.385

# Local raster + vector handling for downstream analysis
rasterio>=1.3
geopandas>=0.14
numpy>=1.24
pandas>=2.0

# Optional but very useful: geemap wraps earthengine-api with
# interactive maps and bulk-download helpers. Uncomment if you want it.
# geemap>=0.32

# Jupyter only if you intend to use the notebook walkthrough
# jupyterlab>=4.0
# ipywidgets>=8.0
